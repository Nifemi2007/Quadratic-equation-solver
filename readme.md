This code solves quadratic equation and indicates whether the solution is a distinct, equal or complex root.

It uses the quadratic formula ax^2 + bx + c = 0.

To get root solution, enter the value of a, b and c

If a == 0, it returns an error that the equation is not a quadratic equation
